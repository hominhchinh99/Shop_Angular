import { ProductQntPipe } from './product-qnt.pipe';

describe('ProductQntPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductQntPipe();
    expect(pipe).toBeTruthy();
  });
});
